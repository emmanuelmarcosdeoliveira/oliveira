---
interface Props {
  children: string;
  color: string;
}
const { children, color } = Astro.props;
---

<div>
  <h2 class=`text-xl font-normal font-mono pb-2 ${color}`>
    {children}
  </h2>
</div>
